资料网站：[[https://huggingface.co/spaces/HuggingFaceFW/blogpost-fineweb-v1]]
参考视频：https://www.youtube.com/watch?v=7xTGNNLPyMI

> [!NOTE]
> **Pre-training（预训练）** ：让模型学会了“语言是什么”，输出是基础模型（Base Model）
> 
> **Post-training**： 则让模型学会了“怎么好好说话、怎么当一个有帮助的助手”。输出训练后的模型，例如（指令微调后的模型）

# 预训练（PreTraing）
## 第一步：数据准备（从网络下载和处理）

 ![[Pasted image 20260111161929.png]]
## 第二步：Tockenization（Tocken化）
![[截屏2026-01-11 16.33.52.png|500]]


![[截屏2026-01-11 16.52.59.png|300]]
## 第三步：神经网络训练

##### 神经网络的输入输出
利用神经网络，建模统计这些tocken的出现顺序，即序列化tockens 。
%% 这里神经网络指的是Transformer 架构的语言模型（GPT、Llama、Qwen、Grok 都算）
 %%
 
==**神经网络的输入是可变长度标记序列**==

解释： 
可变长度这次输入 5 个字，下次 5000 个字都可以，长度不固定
标记序列把文字先切成一个个小块（token），然后排成一个序列

通俗来说：神经网络一次能接受的是一串长度不固定的 token 序列

 ![[截屏2026-01-11 16.47.00.png]]
 如上图，输入是序列的tocken的ID，神经网络预测下一句的概率，其中预测Dieection（4%），Case（2%），**Post（3%）**，我们期望输出是Post，即
 
```
I viewing single post        （我正在查看单个帖子）
```

期望模型输出的是post的概率最大，于是不断调整神经网络，让下一个Post输出概率最大

调整之后：预测Dieection（1%），Case（2%），**Post（4%）**

##### 神经网络
![[截屏2026-01-11 16.58.01.png]]
 
输入的序列tocken的ID，还有神经网络权重，实际上神经网络有数十亿个参数,每一个参数好像DJ的按钮，需要不断的调整参数，让神经网络达到理想输出

![[截屏2026-01-11 16.56.11.png|200]]

中间的数学表达式，抽象理解为Transform，

大模型结构可视化：[[https://bbycroft.net/llm]]

即里面的网络结构
![[截屏2026-01-11 17.30.27.png|100]]

##### 推理（inference）
![[截屏2026-01-11 17.32.02.png]]

输入的是tocken ID（理解为用户对模型输入），这里对应单词“I"

模型预测概率，输出这个单词下一个输出的tocken ID，对应单词“View”

按照这样不断连续预测输出
![[截屏2026-01-11 17.38.21.png|500]]

我们与模型对话相当于模型一直在推理

记住


## Gpt-2模型的训练与推理

模型发布页面：https://openai.com/index/gpt-2-1-5b-release/

模型代码：https://github.com/openai/gpt-2

在src文件夹，里面定义模型的代码，例如**src/model.py**

## 用LLama3 模型举例，对模型输出是概率预测的理解

##### 模型是对已经训练的内容按照模糊记忆根据概率输出

例如模型训练中采用的维基百科的内容训练，当输入维基百科内容片段后，模型可以完整将之后的内容背诵出来，这是对基础模型有效，针对指令微调的模型是不会背诵，回答的更好

如下：输入模型斑马百科的片段文字，模型继续背诵了后面的内容
![[截屏2026-01-11 20.52.32.png]]

##### 模型只能输出知识库内的内容，知识库截止日期后的无法输出
如下，llama知识库截止到2023年，但是2024特朗普当选的维基百科的无法输出

![[截屏2026-01-11 20.53.52.png]]
当输入特朗普的信息，会显示其他信息，这就是模型在进行猜测，不管对不对，只是对内容进行预测输出，也就是导致模型的幻觉
##### 针对基础模型，输入结构化实例化的语言，模型更好理解
 
输入英语与对应的汉语，模型学习规律获得之后的词的预测 
![[截屏2026-01-11 21.03.14.png]]
![[截屏2026-01-11 21.03.14.png]]

所以可以引申出来指令微调，给模型输入很多设定好的问答对，让模型更好的去学习



对基础模型进行指令微调，这也就是涉及到后训练（Post Training）

# 后训练（Post Training）

### 第一步：构建对话数据集

训练数据集与预训练是各种文本只是，后训练则是对话（Conversations）

人工标注员进行构建

![[截屏2026-01-11 21.28.11.png|300]]
### 第二步：按照对话协议输入模型

![[截屏2026-01-11 21.42.47.png]]
 其中标记的im_start还有im_seq是从来没有接受过训练的，是新的tocken，用于给模型提供信息，到哪里是谁说的，类似TCP协议三次握手里面的开始传输的标记（Flags）：SYN 标志，ACK 标志。

这个结构化对象最后被编码成一维的标记序列，也就是输入给模型，用于对模型训练，模型就可以知道哪些是用户问的，那些事他需要回答的，这就是进行后训练的输入部分

**参考论文：**
==《Training language models to follow instructions with human feedback》==
InstructGPT 论文 或 RLHF 论文

首次大规模展示了 Reinforcement Learning from Human Feedback 在对齐语言模型上的威力，导致了 ChatGPT 的诞生

它证明了： 单纯把模型做大（scaling）不够，还需要用人类反馈（human feedback）来 微调（fine-tune），让模型真正学会“听指令、按人类意图行事”。

这是**RLHF（Reinforcement Learning from Human Feedback）【基于人类反馈强化学习】**
方法的经典论文，直接催生了现代所有 Instruct/Chat 模型的训练范式（包括 Grok、Claude、Gemini 等）。


具体位置在3.4章节